# 🧾 PersonalFinanceApp

**PersonalFinanceApp** — это консольное Java-приложение для управления личными финансами.  
Оно позволяет вести учёт доходов и расходов, устанавливать бюджеты по категориям, анализировать состояние бюджета и сохранять данные между сессиями.

---

## 🚀 Основные возможности

| Функция | Описание |
|----------|-----------|
| 👤 **Авторизация** | Регистрация и вход по логину и паролю. Поддержка нескольких пользователей. |
| 💰 **Доходы и расходы** | Добавление операций с указанием категории и суммы. |
| 📊 **Бюджеты** | Установка лимитов по категориям расходов. |
| 💼 **Кошелёк пользователя** | Хранение всех операций и текущего баланса. |
| 📈 **Статистика** | Отображение сводки по категориям, остатка бюджета и баланса. |
| ⚠️ **Оповещения** | Уведомления при превышении бюджета и перерасходе средств. |
| 💾 **Сохранение данных** | Данные пользователя автоматически сохраняются в JSON-файле при выходе. |
| 🔄 **Загрузка при входе** | При авторизации кошелёк восстанавливается из файла. |
| 🧮 **Подсчёты** | Общие и покатегорийные суммы доходов/расходов. |
| 📁 **Экспорт/импорт** | Экспорт отчётов в JSON и возможность восстановления данных. |
| 🧰 **CLI-интерфейс** | Работа через команды в консоли, справка `help`. |

---

## 🧱 Архитектура проекта

Проект построен с использованием **чистой архитектуры (Clean Architecture)**, разделён на логические уровни:

```
┌──────────────────────────────┐
│            CLI               │  ← CommandProcessor — принимает команды пользователя
└──────────────┬───────────────┘
               │
┌──────────────▼───────────────┐
│          Service             │  ← Логика приложения (UserService, WalletService, BudgetService)
└──────────────┬───────────────┘
               │
┌──────────────▼───────────────┐
│          Storage             │  ← Сохранение и загрузка данных (Storage, FileStorage)
└──────────────┬───────────────┘
               │
┌──────────────▼───────────────┐
│           Model              │  ← Модели данных (User, Wallet, Transaction, Budget)
└──────────────────────────────┘
```

📦 **Разделение по слоям в проекте**

```
src
├── main
│   └── java/com/example/finance
│       ├── model         # Модели данных (User, Wallet, Transaction, Budget)
│       ├── service       # Сервисы бизнес-логики (UserService, WalletService, BudgetService)
│       ├── storage       # Хранилище данных (Storage — JSON-сериализация)
│       ├── cli           # Интерфейс командной строки (CommandProcessor)
│       └── Main.java     # Точка входа в приложение
└── test                  # Тесты (JUnit 5)
```

**Архитектурные принципы:**
- Чёткое разделение ответственности (UI / Logic / Storage)
- Возможность подмены хранилища (JSON → база данных)
- Тестируемость сервисов без CLI
- Минимальные зависимости между слоями

---

## ⚙️ Установка и запуск

### 1. Клонировать репозиторий
```bash
git clone <code>
cd personal-finance-app
```

### 2. Собрать проект через Maven
```bash
mvn clean install
```

### 3. Запустить приложение
```bash
mvn exec:java
```

---

## 💡 Поддерживаемые команды (CLI)

| Команда | Пример | Описание |
|----------|---------|----------|
| `register <username>` | `register Ivan` | Регистрация нового пользователя |
| `login <username>` | `login Ivan` | Вход пользователя |
| `logout` | — | Выход из аккаунта |
| `add_income <category>:<amount>` | `add_income Зарплата:40000` | Добавить доход |
| `add_expense <category>:<amount>` | `add_expense Еда:500` | Добавить расход |
| `add_category <name>` | `add_category Транспорт` | Добавить новую категорию |
| `set_budget <category>:<amount>` | `set_budget Еда:4000` | Установить бюджет на категорию |
| `edit_budget <category>:<amount>` | `edit_budget Еда:5000` | Изменить бюджет на категорию |
| `summary` | — | Показать сводку доходов, расходов и бюджета |
| `export_json` | — | Экспортировать данные в JSON |
| `help` | — | Список команд |
| `exit` | — | Сохранить данные и выйти |

---

## 📊 Пример вывода

```
[Sergey] > summary
----- Сводка для пользователя: Sergey -----
Общий доход: 63,000.00
Доходы по категориям:
  Зарплата: 60,000.00
  Бонус: 3,000.00
Общие расходы: 8,300.00
Бюджет по категориям:
  Еда: 4,000.00, Оставшийся: 3,200.00
  Развлечения: 3,000.00, Оставшийся: 0.00
  Коммунальные: 2,500.00, Оставшийся: -500.00
Баланс кошелька: 54,700.00
⚠ ВНИМАНИЕ: Бюджет по категории 'Коммунальные' превышен на 500.00
```

---

## 🧪 Тестирование

Запуск тестов:
```bash
mvn test
```

**Фреймворк:** JUnit 5  
**Покрытие:**  
- `WalletServiceTest` — проверяет корректность баланса при доходах и расходах.  
- `BudgetServiceTest` — проверяет добавление категории и установку бюджета.
- `SummaryCalculationTest` — проверяет вычисление итогов по категориям доходов и расходов.
- `BudgetLimitAlertTest` — проверяет превышение лимита по категории.
- `ValidationTest` — проверяет валидацию пользовательского ввода.

---

## 📦 Экспорт и хранение данных

- Каждый пользователь имеет собственный JSON-файл:
- Данные сохраняются при выходе из приложения.  
- При следующем входе они автоматически загружаются.

---

## 🧰 Технические детали

- Все данные хранятся **в оперативной памяти** во время работы.
- Используется библиотека **Gson** для сериализации/десериализации JSON.
- CLI поддерживает пользовательские ошибки и валидацию команд.
- При превышении бюджета выводятся оповещения.

---

## ⚙️ Автоматизация и CI

Для автоматической проверки сборки и тестов добавлен GitHub Actions:  
**.github/workflows/maven.yml**

```yaml
name: Java CI

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'
      - name: Build and test
        run: mvn clean verify
```